# baseline_wrappers

Baseline wrappers for interval kernels. These expose `*_prec` functions with outward rounding and optional precision selection.

## Purpose

Provide a consistent API for midpoint evaluation followed by outward rounding. No analytic bounds or sampling are applied here.
