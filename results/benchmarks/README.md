# Benchmarks

This folder stores benchmark runs created by `benchmarks/bench_harness.py`. Each run lives in a timestamped subfolder with per-sweep subfolders.

By default, benchmark run artifacts are gitignored. Keep only this README and selected curated summaries when needed.
