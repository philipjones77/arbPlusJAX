# Example API Surface Inputs

This folder owns tracked inputs for `example_api_surface.ipynb`.

The notebook currently uses repo-local generated benchmark artifacts rather than
static input fixtures.
