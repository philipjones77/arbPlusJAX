# Example Inputs

This directory is the shared root for tracked example-owned inputs.

Rules:

- each example owns a single named subfolder
- do not place ad hoc files directly in this directory
- keep reusable templates in the owning example subfolder
- avoid duplicating equivalent templates across multiple example folders

Current example-owned input folders include:

- `example_run_suite/`
