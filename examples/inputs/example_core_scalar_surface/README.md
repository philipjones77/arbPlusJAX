# Example Core Scalar Surface Inputs

This folder owns tracked inputs for `example_core_scalar_surface.ipynb`.

The notebook currently constructs its demonstration inputs in-code and writes
generated artifacts under the matching output root.
