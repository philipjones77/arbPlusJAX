# Example Outputs

This directory is the shared root for retained example-owned outputs.

Rules:

- each example owns a single named subfolder
- do not place ad hoc files directly in this directory
- retained summaries should live in the owning example subfolder
- transient local-only outputs should not be committed unless intentionally retained

Current example-owned output folders include:

- `example_run_suite/`
