from __future__ import annotations

import argparse
import json
import os
from pathlib import Path
import subprocess
import sys
import time


REPO_ROOT = Path(__file__).resolve().parents[1]


def _base_env() -> dict[str, str]:
    env = os.environ.copy()
    env["PYTHONPATH"] = str(REPO_ROOT / "src") + os.pathsep + env.get("PYTHONPATH", "")
    env.setdefault("JAX_PLATFORMS", "cpu")
    return env


def _run_import_probe(module_expr: str) -> dict[str, float]:
    code = (
        "import time\n"
        "t0=time.perf_counter()\n"
        f"{module_expr}\n"
        "print(time.perf_counter()-t0)\n"
    )
    completed = subprocess.run(
        [sys.executable, "-c", code],
        cwd=REPO_ROOT,
        env=_base_env(),
        capture_output=True,
        text=True,
        check=True,
    )
    seconds = float(completed.stdout.strip().splitlines()[-1])
    return {"seconds": round(seconds, 6)}


def _run_worker(family: str, pad_to: int, n_terms: int) -> dict[str, object]:
    env = _base_env()
    completed = subprocess.run(
        [
            sys.executable,
            str(Path(__file__).resolve()),
            "--worker",
            "--family",
            family,
            "--pad-to",
            str(pad_to),
            "--n-terms",
            str(n_terms),
        ],
        cwd=REPO_ROOT,
        env=env,
        capture_output=True,
        text=True,
        check=True,
    )
    return json.loads(completed.stdout)


def _worker(family: str, pad_to: int, n_terms: int) -> None:
    import jax
    import jax.numpy as jnp

    from arbplusjax import api

    if family != "acb_dirichlet_zeta":
        raise ValueError(f"unsupported family {family!r}")

    s = jnp.array([0.75 + 2.0j, 0.8 + 2.5j], dtype=jnp.complex64)

    t0 = time.perf_counter()
    _ = jax.devices()
    t1 = time.perf_counter()

    bound = api.bind_point_batch_jit(family, pad_to=pad_to, n_terms=n_terms)
    t2 = time.perf_counter()
    out = bound(s)
    jax.block_until_ready(out)
    t3 = time.perf_counter()

    t4 = time.perf_counter()
    out = bound(s)
    jax.block_until_ready(out)
    t5 = time.perf_counter()

    payload = {
        "family": family,
        "pad_to": pad_to,
        "n_terms": n_terms,
        "backend_init_s": round(t1 - t0, 6),
        "compile_plus_first_point_batch_s": round(t3 - t2, 6),
        "steady_point_batch_s": round(t5 - t4, 6),
    }
    print(json.dumps(payload, indent=2))


def main() -> None:
    parser = argparse.ArgumentParser()
    parser.add_argument("--worker", action="store_true")
    parser.add_argument("--family", choices=("acb_dirichlet_zeta",), default="acb_dirichlet_zeta")
    parser.add_argument("--pad-to", type=int, default=4)
    parser.add_argument("--n-terms", type=int, default=32)
    parser.add_argument(
        "--out-json",
        default="benchmarks/results/dirichlet_point_startup_probe/dirichlet_point_startup_probe.json",
    )
    parser.add_argument(
        "--out-md",
        default="benchmarks/results/dirichlet_point_startup_probe/dirichlet_point_startup_probe.md",
    )
    args = parser.parse_args()

    if args.worker:
        _worker(args.family, args.pad_to, args.n_terms)
        return

    key = f"{args.family}_point_path"
    payload = {
        "import_arbplusjax_api": _run_import_probe("from arbplusjax import api"),
        key: _run_worker(args.family, args.pad_to, args.n_terms),
    }

    out_json = REPO_ROOT / args.out_json
    out_md = REPO_ROOT / args.out_md
    out_json.parent.mkdir(parents=True, exist_ok=True)
    out_md.parent.mkdir(parents=True, exist_ok=True)
    out_json.write_text(json.dumps(payload, indent=2) + "\n", encoding="utf-8")
    out_md.write_text(
        "\n".join(
            [
                "Last updated: 2026-03-26T00:00:00Z",
                "",
                "# Dirichlet Point Startup Probe",
                "",
                "Generated by `benchmarks/dirichlet_point_startup_probe.py`.",
                "",
                f"- api import s: `{payload['import_arbplusjax_api']['seconds']}`",
                f"- family: `{payload[key]['family']}`",
                f"- pad_to: `{payload[key]['pad_to']}`",
                f"- n_terms: `{payload[key]['n_terms']}`",
                f"- backend init s: `{payload[key]['backend_init_s']}`",
                f"- compile plus first point batch s: `{payload[key]['compile_plus_first_point_batch_s']}`",
                f"- steady point batch s: `{payload[key]['steady_point_batch_s']}`",
                "",
            ]
        )
        + "\n",
        encoding="utf-8",
    )
    print(f"Wrote: {out_json}")
    print(f"Wrote: {out_md}")


if __name__ == "__main__":
    main()
